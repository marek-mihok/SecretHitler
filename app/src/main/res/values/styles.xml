<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/backgroundOrange</item>
        <item name="colorPrimaryDark">@color/secretGray</item>
        <item name="colorAccent">@color/secretWhite</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme.NoActionBarNoFullScreen" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/backgroundOrange</item>
        <item name="colorPrimaryDark">@color/secretGray</item>
        <item name="colorAccent">@color/secretWhite</item>
    </style>

    <!--  Edit text theme  -->
    <style name="EditTextTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorControlNormal">@color/lightGray</item>
        <item name="colorControlActivated">@color/lightGray</item>
        <item name="colorControlHighlight">@color/lightGray</item>
    </style>

    <style name="ButtonMainMenu">
        <item name="android:background">@drawable/button_main_menu</item>
        <item name="android:fontFamily">@font/rokkitt_bold</item>
        <item name="android:textColor">@color/backgroundOrange</item>
        <item name="android:textSize">@dimen/fontSizeMenu</item>
    </style>

    <style name="PolicyCardSmall">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_marginStart">8dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:clipChildren">false</item>
        <item name="android:clipToPadding">false</item>
    </style>

    <style name="SettingsSwitch" parent="Theme.AppCompat.Light">
        <item name="colorControlActivated">@color/secretGray</item>
    </style>

    <!-- The launcher theme. It sets the main window background to the launch_screen drawable -->
    <style name="AppTheme.NoActionBar.Launcher">
        <item name="android:windowBackground">@drawable/launch_screen</item>
    </style>

</resources>
